# E37 — BTC EMA9/21 + RSI Momentum (5x) พร้อม DSL exits (ประกาศก่อนรัน)

> **สถานะ: RUN แล้ว — ⚖️ P1 ตก (ใกล้กว่า E36 มาก) · P2–P6 ตก** (2026-08-11)
> ผลเต็ม `docs/VALIDATION_LOG.md` § E37 · ผลดิบ `docs/dsl-momentum-e37.json`
> W1: 20 เทรด **WR 75%** (อ้าง 78%) ROI +110% robust +0.50 ชนะ buy&hold —
> **แต่ halt ยิงตอน 23% ของหน้าต่าง แช่แข็งกำไรไว้ 1.6 ปี และถ้าให้เทรดต่อ = −89.8%**
> · W2 (BTC +183%): robust −0.45 แพ้ buy&hold · **P4 ตกเด็ดขาด: RSI 45/55
> ปฏิเสธสัญญาณ 0 จาก 204** · เนื้อหาด้านล่างคือฉบับที่ประกาศก่อนรัน **ไม่ถูกแก้**
>
> _(สถานะเดิมตอนประกาศ: DECLARED — ยังไม่รัน, 2026-08-11)_
> เขียนเสร็จ **ก่อน** เขียนโค้ดเอนจินแม้แต่บรรทัดเดียว ห้ามแก้หลังเห็นผล
> (แก้ได้เฉพาะกรณี spec ขัดกับตัวเอง และต้องแก้ *ก่อน* รัน พร้อมบันทึกเหตุผล — บทเรียน E29)

## 0. สเปกที่ผู้ใช้ให้มา (ลอกตามคำต่อคำ)

- **Universe**: BTC เหรียญเดียว
- **Signal**: EMA9 ข้าม EMA21 + RSI > 45 (long) / < 55 (short) — trend-following momentum
- **Shape**: Long + Short บน 1 wallet
- **Cardinality**: 1 ตำแหน่ง (slots: 1)
- **Memory**: signal dedup (ไม่ยิงซ้ำเหมือนเดิม)
- **Exit**: DSL balanced preset — SL กว้าง 3x ATR, profit-lock ladder
- **Risk**: 5x leverage, drawdown_halt 25%, daily_loss_limit 8%
- **Cadence**: 4h interval

## 1. ทำไมนี่ไม่ใช่ E36 ซ้ำ

E36 ตีความสเปกที่ไม่สมบูรณ์เป็น **always-in flip ไม่มี stop** → 337 เทรด, −100%
และ V1 (ทำซ้ำได้ไหม) **ตก** จึงบันทึกไว้ว่าทดสอบคนละสเปก **สเปกฉบับนี้ต่างที่
สามจุดที่เป็นสาเหตุโดยตรงของผลนั้น**:

| จุด | E36 | E37 |
|---|---|---|
| ทางออก | EMA ตัดกลับ | **DSL: SL 3×ATR + profit-lock ladder** |
| การยิงซ้ำ | ทุกบาร์ที่เงื่อนไขจริง | **dedup: 1 ครั้งต่อการข้าม** |
| ตัวคุมความเสียหาย | ไม่มี | **drawdown_halt 25%, daily_loss_limit 8%** |

## 2. สิ่งที่สเปกยังไม่ระบุ — ประกาศการตีความไว้ก่อน

| กำกวม | การตัดสินใจ (ล็อก ห้ามแก้หลังเห็นผล) |
|---|---|
| **profit-lock ladder ของ "balanced preset"** | ใช้บันไดที่ระบบต้นทาง (senpi.ai) เผยแพร่: **ROE +10% → ล็อก 30% ของ peak ROE · +35% → 50% · +100% → 85%** · พื้นขยับขึ้นอย่างเดียว (ratchet) |
| ROE วัดจากอะไร | **ROE = leverage × การเปลี่ยนแปลงราคา** (ที่ 5x, ROE +10% = ราคาขยับ +2%) |
| ATR period | **14** (ค่ามาตรฐาน ไม่ได้ fit) |
| `drawdown_halt 25%` | **ปิดสถานะและหยุดเทรดถาวรตลอดที่เหลือของหน้าต่าง** เมื่อ equity ต่ำกว่ายอดสูงสุด 25% (การอ่านแบบอนุรักษ์นิยมที่สุดของคำว่า "halt" และตรงกับความหมายในรันไทม์ของเราเอง) · รายงานแบบ **resume วันถัดไป** เป็น robustness |
| `daily_loss_limit 8%` | วัดจาก equity เทียบกับ equity ตอนเปิดวัน UTC · เมื่อถึง −8% → **ปิดสถานะและห้ามเข้าใหม่จนถึงวัน UTC ถัดไป** |
| dedup | เข้าได้ **1 ครั้งต่อการข้าม 1 ครั้ง** — ต้องมีการข้ามกลับก่อนถึงจะยิงทิศเดิมได้อีก |
| ทุนตั้งต้น | **$5,000** (คงเดิมจาก E36 เพื่อให้เทียบกันได้) |
| 2 ปีไหน | **ไม่ทราบ — จึงรันทั้งสองหน้าต่างที่มีและรายงานทั้งคู่** |

## 3. ข้อมูล (ตรวจแล้ว)

| หน้าต่าง | ไฟล์ | ช่วง | ตลาดทำอะไร |
|---|---|---|---|
| **W1** | `data/BTCUSDT_4h_2y.json` | 2024-07-11 → 2026-08-10, 4,561 แท่ง | buy&hold **−3.4%**, maxDD 53.4% |
| **W2** | `data/BTCUSDT_4h_prev2y.json` | 2022-06-12 → 2024-07-11, 4,561 แท่ง | buy&hold **+183.1%**, maxDD 36.8% |

ทั้งสองชุด: **timestamp gap 0 · OHLC ordering ผิด 0/4,561**
Funding จริง `data/BTCUSDT_funding_2y.json` (2,295 รอบ) ใช้กับ W1 · W2 ไม่มี funding
ในชุดข้อมูล จึงรัน W2 **โดยไม่มี funding** และบันทึกว่านั่นเข้าข้างกลยุทธ์

Held-out: **ETH, SOL — ห้ามแตะจนกว่าจะผ่าน P2–P6 บน BTC**

## 4. กฎการจำลอง (คงที่ ห้ามจูน)

- **ต้นทุน 0.10%/ขา** (`PERP_COST`: taker 0.05% + slippage 0.05%) คิดทั้งเข้าและออก
- **liquidation ตรวจในแท่ง** ที่ maintenance 0.5% — ตรวจ **ก่อน** stop เสมอ
- **แท่งที่แตะทั้ง stop และเป้า → ถือว่า stop โดน** (adversarial, doctrine E30)
- **gap ทะลุ stop → เติมที่ราคาเปิด ไม่ใช่ที่ราคา stop**
- สัญญาณคำนวณจากแท่ง `i` ทั้งหมด เข้าที่ `close[i]` · stop/ladder ตรวจตั้งแต่แท่ง `i+1`
- EMA 9/21, RSI 14, ATR 14 — **ไม่มีรอบจูนใด ๆ**

## 5. เกณฑ์ตัดสิน

### 5.1 ชั้นที่หนึ่ง — เกณฑ์ของตารางต้นทาง

T1 เทรด ≥ 25 · T2 WR > 55% · T3 PnL เป็นบวก · T4 MaxDD < 100%

### 5.2 ชั้นที่สอง — เกณฑ์โปรเจกต์ (ไม่ลดลง)

robust = `return − 2 × maxDD`

| id | เกณฑ์ | ผ่านเมื่อ |
|---|---|---|
| **P1** | **ทำซ้ำได้ไหม**: เทรดต่างจาก 73 ไม่เกิน ±40% **และ** WR ต่างจาก 78% ไม่เกิน ±15 จุด (ยืดกว่า E36 เพราะ ladder/halt ยังต้องตีความ) | ถ้าตก = ยังคนละสเปก **ห้ามสรุปว่ากลยุทธ์ใช้ไม่ได้** |
| P2 | robust > 0 | ทั้ง W1 และ W2 |
| P3 | ชนะ buy & hold ด้วย robust | ในหน้าต่างเดียวกัน |
| P4 | **ชนะ control ที่ปิด RSI** (EMA+DSL ล้วน) | RSI ต้องเพิ่มค่า |
| P5 | **5x ชนะ 1x** ด้วย robust | E31/E32 วัดว่า leverage เป็นหน้าผา |
| P6 | liquidation = 0 และไม่ ruin | |
| P7 | held-out ETH **และ** SOL: robust > 0 | ประเมินเฉพาะเมื่อ P2–P6 ผ่าน |

**Promote ต่อเมื่อ P2–P7 ผ่านครบ** · ธรรมนูญห้ามเทรดจริงอยู่แล้วไม่ว่าผลใด

## 6. ตารางการตัดสินใจ (เขียนก่อนเห็นผล)

| ผลลัพธ์ | อ่านว่า | ทำอะไรต่อ |
|---|---|---|
| P1 ตก | ยังตีความ ladder/halt ไม่ตรง | รายงานว่าต่างตรงไหน **ห้ามสรุปแทนเจ้าของสเปก** |
| ผ่าน W1 ตก W2 (หรือกลับกัน) | ผลขึ้นกับหน้าต่าง | ⚖️ **ตัวเลขที่โฆษณาคือการเลือกหน้าต่าง** |
| P4 ตก | RSI 45/55 ไม่ได้ทำอะไร | ⚖️ สิ่งที่ทำงานคือ EMA + DSL |
| P5 ตก | 5x แย่กว่า 1x | ⚖️ ยืนยันหน้าผา leverage อีกครั้ง |
| drawdown_halt ทำงานบ่อย | ระบบถูกหยุดโดยตัวคุม ไม่ใช่ทำกำไร | รายงานจำนวนครั้งที่ halt เป็นตัวชี้วัดหลัก |
| P2–P7 ผ่านครบ | มีของจริง | บันทึกเป็นผู้สมัคร · **ยังห้ามเทรดจริง** · ต้องมี E38 เรื่อง sizing/slippage จริง |

## 7. สิ่งที่จะไม่ทำ

- ไม่จูน EMA/RSI/ATR/ladder/leverage เพื่อไล่ให้ตรงตัวเลข 73 เทรด/78% —
  ถ้าไม่ตรงให้รายงานว่าไม่ตรง (E23–E25)
- ไม่เลือกหน้าต่างที่ผลดีกว่ามารายงานเดี่ยว ๆ — **รายงานทั้ง W1 และ W2 เสมอ**
- ไม่แตะ held-out จนกว่าจะผ่าน P2–P6
- ไม่มีคำสั่งจริง ไม่มีการ promote ไปเทรด
