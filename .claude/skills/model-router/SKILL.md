# Model router skill

Do not restate rules — read `ROUTING.md` and run:

```
python agent/router.py "task text here"
```

Effort before model. Router guilty until it beats best-single-model on `gate/eval_gate.py` traffic.
Watch weekly: cheap-tier share of work.
